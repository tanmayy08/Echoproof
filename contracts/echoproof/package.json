{
  "name": "@echoproof/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "EchoProof Compact contracts.",
  "type": "module",
  "scripts": {
    "build": "compact compile --skip-zk src/EchoProof.compact managed",
    "build:keys": "compact compile src/EchoProof.compact managed",
    "test": "node --test \"test/*.test.mjs\""
  }
}
